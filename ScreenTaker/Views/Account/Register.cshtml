@model ScreenTaker.Models.RegisterViewModel
@{
    ViewBag.Title = "Register";
}

<div class="container" style="margin-top:40px;">

    <div class="row">
        <h2 class="text-center">@ViewBag.Title</h2>
        <div class="col-sm-4">
        </div>
        <div class="col-sm-4 account-style shadow center-block">
            @*<section id="regiterForm">
                @using (Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "col-md-12 text-center form-horizontal", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    <h4>Create a new account to keep track of your pasted images</h4>
                    <hr />
                    @Html.ValidationSummary("", new { @class = "text-left text-primary", @style = "padding: 0" })
                    <div class="form-group">
                        <div class="col-md-12">
                            @Html.TextBoxFor(m => m.Email, new { @class = "input-lg", @placeholder = "Email", @style = "border-radius: 0" })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-12">
                            @Html.PasswordFor(m => m.Password, new { @class = "input-lg", @placeholder = "Password", @style = "border-radius: 0" })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-12">
                            @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "input-lg", @placeholder = "Confirm password", @style = "border-radius: 0" })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-12">
                            <input type="submit" class="btn btn-block btn-lg btn-default account-button" value="Register" />
                        </div>
                    </div>
                }
            </section>*@
            <section id="loginForm">
                @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "col-md-12 text-center form-horizontal", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    <h4>Create a new account to keep track of your pasted images</h4>
                        <hr />
                        @Html.ValidationSummary(true, "", new { @class = "text-left text-primary" })
                        <div class="form-group">
                            <div class="col-md-12 login-signup-center">
                                @Html.TextBoxFor(m => m.Email, new { @class = "form-control input-lg", @style = "border-radius: 0", @placeholder = "Email" })
                                @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-left text-primary" })
                            </div>
                        </div>
                            <div class="form-group ">
                                <div class="col-md-12 login-signup-center">
                                    @Html.PasswordFor(m => m.Password, new { @class = "form-control input-lg", @style = "border-radius: 0", @placeholder = "Password" })
                                    @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-left text-primary" })
                                </div>
                            </div>
                            <div class="form-group ">
                                <div class="col-md-12 login-signup-center">
                                    @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control input-lg", @style = "border-radius: 0", @placeholder = "Confirm password" })
                                    @Html.ValidationMessageFor(m => m.ConfirmPassword, "", new { @class = "text-left text-primary" })
                                </div>
                            </div>
                            <div class="form-group ">
                                <div class="col-md-12 login-signup-center">
                                    <input type="submit" value="Sign up" class="btn btn-block btn-lg btn-default account-button" />
                                </div>
                            </div>
                            <div class="text-center">
                                <h4>
                                    @Html.ActionLink("Already have an account?", "Login", "Account", new { @class = "account-link" })
                                </h4>
                            </div>
                }
            </section>
        </div>
        <div class="col-sm-4">
        </div>
    </div>
</div>


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
